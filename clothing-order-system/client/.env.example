# Backend API origin only (scheme + host + optional port).
# Local: http://localhost:4000
# Production (Vercel): https://oms-28nk.onrender.com
# No trailing slash. No /api suffix — paths in api.ts already start with /api/...
VITE_API_URL=http://localhost:4000
